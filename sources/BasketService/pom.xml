<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jooby</groupId>
    <artifactId>jooby-project</artifactId>
    <version>1.0.0.CR2</version>
  </parent>

  <artifactId>BasketService</artifactId>
  <groupId>info.unterstein</groupId>
  <version>1.0-SNAPSHOT</version>

  <name>BasketService</name>
  <description>generated by Jooby archetype</description>

  <properties>
    <jooby.version>1.0.0.CR2</jooby.version>

    <!-- Startup class -->
    <application.class>info.unterstein.BasketService</application.class>
  </properties>

  <dependencies>
    <dependency>
      <groupId>info.unterstein</groupId>
      <artifactId>ServiceCommons</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>

    <!-- Jooby -->
    <dependency>
      <groupId>org.jooby</groupId>
      <artifactId>jooby</artifactId>
    </dependency>

    <!-- Server -->
    <dependency>
      <groupId>org.jooby</groupId>
      <artifactId>jooby-netty</artifactId>
    </dependency>

    <!-- logging -->
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
    </dependency>

    <!-- Tests -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.jooby</groupId>
      <artifactId>jooby</artifactId>
      <scope>test</scope>
      <classifier>tests</classifier>
    </dependency>

    <!-- Fluent HC -->
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>fluent-hc</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpmime</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <!-- Build fat jar -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.jooby</groupId>
        <artifactId>jooby-maven-plugin</artifactId>
        <version>1.0.0.CR2</version>
        <configuration>
          <mainClass>${application.class}</mainClass>
          <commands>
          </commands>
          <compiler>on</compiler>
          <vmArgs></vmArgs>
          <debug>false</debug>
          <includes>
            <include>**/*.class</include>
            <include>**/*.conf</include>
            <include>**/*.properties</include>
          </includes>
          <excludes>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
